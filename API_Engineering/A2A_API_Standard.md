# A2A (Agent-to-Agent) API 標準

## 簡介
在 2026 年，AI 代理人之間的通訊將不再依賴傳統的 JSON 格式，取而代之的是一種新的二進制流協議，旨在提高效率和降低延遲。

## 二進制流
使用二進制流可以減少資料的大小和傳輸時間，適合高頻交易和即時通訊的應用場景。

```rust
struct BinaryMessage {
    header: Header,
    payload: Vec<u8>,
}
```

## 自我描述型接口
自我描述型接口允許代理人透過協議中的元資料來理解請求/響應格式，有助於動態適應新的 API。

---